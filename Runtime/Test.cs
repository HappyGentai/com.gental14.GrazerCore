using UnityEngine;

namespace GrazerCore
{
    public class Test : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("Hi, this is my custom package test script!");
        }

        // Update is called once per frame
        void Update()
        {

        }
    }

}